#include "main.h"

/**
 * mul - returns multiplication of two integers
 *
 * @a: one of the integer
 *
 * @b: one of the integer
 *
 * Return: 0
 */
int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
}
